<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <title>Edit Article</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
  <%- include('../layout') %>

    <h1>Edit Article</h1>

    <% if (error) { %>
      <p style="color:red;">
        <%= error %>
      </p>
      <% } %>

        <form action="/articles/<%= article.id %>/edit" method="POST" enctype="multipart/form-data">
          <div class="form-group">
            <label>Title</label>
            <input type="text" name="title" value="<%= article.title %>" required />
          </div>
          <div class="form-group">
            <label>Cover Image (optional)</label>
            <% if (article.image) { %>
              <div style="margin-bottom: 0.5rem;">
                <img src="<%= article.image %>" alt="Current Image"
                  style="max-height: 100px; border-radius: var(--radius);">
              </div>
              <% } %>
                <input type="file" name="image" accept="image/*"
                  style="border: 1px solid var(--border); padding: 0.5rem; width: 100%; border-radius: var(--radius);">
          </div>
          <div>
            <label>Content:</label>
            <textarea name="content" required><%= article.content %></textarea>
          </div>
          <button type="submit">Save</button>
        </form>
</body>

</html>