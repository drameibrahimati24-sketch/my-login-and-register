<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <title>My Profile</title>
    <!-- Styles in layout -->
</head>

<body>
    <%- include('../layout') %>
        <div class="page-container">
            <div
                style="max-width: 600px; margin: 0 auto; background: white; padding: 2rem; border-radius: var(--radius); border: 1px solid var(--border);">
                <h1 style="margin-bottom: 2rem; font-size: 1.75rem; font-weight: 700;">My Profile</h1>

                <% if (error) { %>
                    <div
                        style="background: #fef2f2; color: #b91c1c; padding: 0.75rem; border-radius: var(--radius); margin-bottom: 1.5rem;">
                        <%= error %>
                    </div>
                    <% } %>
                        <% if (success) { %>
                            <div
                                style="background: #ecfdf5; color: #047857; padding: 0.75rem; border-radius: var(--radius); margin-bottom: 1.5rem;">
                                <%= success %>
                            </div>
                            <% } %>

                                <form action="/profile" method="POST">
                                    <div class="form-group">
                                        <label>Full Name</label>
                                        <input type="text" name="name" value="<%= user.name || '' %>"
                                            placeholder="Your Name" />
                                    </div>
                                    <div class="form-group">
                                        <label>Email Address</label>
                                        <input type="email" name="email" value="<%= user.email %>" required />
                                    </div>
                                    <div class="form-group">
                                        <label>Bio</label>
                                        <textarea name="bio" rows="4"
                                            placeholder="Tell us a bit about yourself..."><%= user.bio || '' %></textarea>
                                        <small style="color: var(--text-muted);">This will be displayed on your profile
                                            (future feature).</small>
                                    </div>
                                    <div style="display: flex; justify-content: flex-end; margin-top: 1.5rem;">
                                        <button type="submit" class="btn btn-primary">Save Changes</button>
                                    </div>
                                </form>
            </div>
        </div>
</body>

</html>