const express = require('express');
const router = express.Router();

// API Admin Endpoints (Skeleton)
router.get('/users', (req, res) => { res.json({ message: 'Get Users API' }) });

module.exports = router;
