const express = require('express');
const router = express.Router();

// API Vacancies Endpoints (Skeleton)
router.get('/', (req, res) => { res.json({ message: 'Get Vacancies API' }) });

module.exports = router;
